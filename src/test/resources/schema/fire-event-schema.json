{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["id", "sensorId", "status", "description", "timestamp"],
  "properties": {
    "id": {"type": "string"},
    "sensorId": {"type": "string"},
    "status": {"type": "string", "enum": ["ALERT", "RESOLVED"]},
    "description": {"type": "string"},
    "timestamp": {"type": "string"}
  }
}